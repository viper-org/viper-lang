#include "test.vh"

#define macro 24

func @test(a: i32) -> i32 {
    return a + 2;
}

func @main() -> i32 {
    let str: i8* = "hello";
    let b: i32 = test(12);
    if (b == 44) {
        b = b + 6;
    }
    let ptr: i32* = &b;
    return *ptr;
}