extern @SystemCall(i64, i64, i64, i64) -> i64;

@StrLen(i8* data) -> i32
{
    i32 i = 0;
    while(*(data + i))
        i = i + 1;
    return i;
}

@WriteString(i8* data) -> void
{
    SystemCall(1, 1, data, StrLen(data));
    return;
}