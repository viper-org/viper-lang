struct String
{
    i8* data;
    i32 size;
}

@BuildString(i8* data) -> String
{
    String result;
    result.data = data;
    result.size = StrLen(data);
    return result;
}

@main() -> i32
{
    String test = BuildString("Hello World!");
    WriteString(test.data);
    return 0;
}