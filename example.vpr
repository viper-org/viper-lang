import void WriteString(int8* str, int32 size);

let int32 Main() = {
    let int32 x = 1;
    if(x)
        WriteString("Hello World!\n", 13);
    else
        WriteString("False\n", 6);
    return 0;
}